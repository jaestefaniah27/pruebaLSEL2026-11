{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ejecutar Tests Ceedling",
            "type": "shell",
            "command": "ruby -S ceedling test:all",
            "options": {
                "cwd": "${workspaceFolder}/ceedling"
            },
            "group": "test",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}